name: Install Python dependencies

runs:
  using: composite
  steps:
    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        python -m pip install --group dev
      shell: bash
